{
  "name": "blockchain-generation",
  "version": "1.0.0",
  "description": "Generate blockchain from blocks.json transaction definitions",
  "main": "generate-blocks.js",
  "scripts": {
    "generate": "node generate-blocks.js"
  },
  "dependencies": {
    "web3": "^4.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
